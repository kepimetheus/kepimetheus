apiVersion: v1
kind: Service
metadata:
  name: {{ include "epimetheus.fullname" . }}
  labels:
    {{- include "epimetheus.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "epimetheus.selectorLabels" . | nindent 4 }}
