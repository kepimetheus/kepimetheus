{{- if .Values.provider.secret.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "kepimetheus.secretName" . }}
  labels:
    {{- include "kepimetheus.labels" . | nindent 4 }}
  {{- with .Values.provider.secret.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
type: Opaque
data:
  AWS_ACCESS_KEY_ID: {{ .Values.provider.secret.data.AWS_ACCESS_KEY_ID | b64enc }}
  AWS_SECRET_ACCESS_KEY: {{ .Values.provider.secret.data.AWS_SECRET_ACCESS_KEY | b64enc }}
{{- end }}